# .agents/ bus runtime state (SPEC v0.1 LAYOUT-3) — committed: inbox/*.md, done/*.md, .gitkeep
.agents/claimed/
.agents/approvals/
.agents/events.jsonl
.agents/council/
.agents/annotations/

# machine-local Claude Code config (headroom proxy env + hooks)
.claude/settings.local.json
